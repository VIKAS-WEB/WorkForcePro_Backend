server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/workforce_pro
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: workforceprosupersecurekey1234567890
